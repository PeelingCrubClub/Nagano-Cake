 <div class="container">
 <%= render "shared/flash_message" %>
<div class="row mb-4">
  <div class="col-md-10 col-lg-12 mx-auto">
  <div class = "main-container">
      <div class='row mb-5 mt-5'>
        <div class='nagano-text'>
          <h5 class="font-weight-bold">ようこそ、ながのCAKEへ！</h5><br>
          このサイトでは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </div>
      </div>
   </div>

  <div class='row'>
    <div class='col-md-6 offset-md-3 mt-5 h5 text-center'>新着商品</div>
  </div>
  <div class='row'>
    <% @items.each do |item| %>
    <div class='row float-right a'>
      <table class="table table-borderless ml-4">
        <tr><td>
          <%= link_to item_path(item.id) do %>
           <%= attachment_image_tag item, :image, format: 'jpg' , fallback: "no_image.jpg", size: "200x180" %>
           <% end %>
        </td></tr>
        <tr><td><%= item.item_name %></td></tr>
         <tr><td>¥<%= item.price %></td></tr>
      </table>
     </div>
    <% end %>
  </div>
     <div class="text-right">
      <%= link_to items_path do%>
      すべての商品を見る<i class="fas fa-search"></i>
      <% end %>
     </div>
   
 </div>
</div>